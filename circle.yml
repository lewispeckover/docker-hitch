--- 
machine:
  services:
    - docker
checkout:
  post:
    - rm -rf ./hitch/.abuild
    - sudo docker run -it --rm -v `pwd`/hitch:/build lewispeckover/apkbuilder
compile:
  override:
    - sudo docker build .
